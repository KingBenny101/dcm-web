function handleBubbleClick(index) {
    // open the modal
    // get the content/plan value
    // close the modal
    // change Card to Iframe
    console.log(index);
}


export default handleBubbleClick;